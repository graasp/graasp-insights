const { GRAASP_SCHEMA_ID } = require('../../shared/constants');
const GRAASP_SCHEMA = require('../../shared/data/graaspSchema');

const DEFAULT_SCHEMAS = {
  [GRAASP_SCHEMA_ID]: {
    id: GRAASP_SCHEMA_ID,
    label: 'Graasp',
    description:
      'Schema for datasets generated by the Graasp educational platform',
    schema: GRAASP_SCHEMA,
    tagStyle: {
      backgroundColor: '#5050d2',
      color: 'white',
    },
  },
};

module.exports = { DEFAULT_SCHEMAS };
