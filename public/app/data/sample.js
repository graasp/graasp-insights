const {
  DATASETS_COLLECTION,
  ALGORITHMS_COLLECTION,
  EXECUTIONS_COLLECTION,
  SETTINGS_COLLECTION,
  SCHEMAS_COLLECTION,
} = require('../db');
const { DEFAULT_FILE_SIZE_LIMIT } = require('../../shared/constants');

module.exports = {
  [DATASETS_COLLECTION]: [],
  [ALGORITHMS_COLLECTION]: [],
  [EXECUTIONS_COLLECTION]: [],
  [SETTINGS_COLLECTION]: {
    fileSizeLimit: DEFAULT_FILE_SIZE_LIMIT,
  },
  [SCHEMAS_COLLECTION]: {},
};
